pyyaml>=6.0
httpx>=0.27.0
python-dotenv>=1.0.0

# FastAPI agent API service
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# LLM provider — set env vars before running:
#   OPENAI_API_KEY=sk-...          (required for live LLM)
#   OPENAI_MODEL=gpt-4o            (optional, default: gpt-4o)
